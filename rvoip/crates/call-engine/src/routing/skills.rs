/// Skill matcher for agent-call matching
pub struct SkillMatcher {
    // TODO: Implement skill matching logic
}

impl SkillMatcher {
    pub fn new() -> Self {
        Self {}
    }
}

impl Default for SkillMatcher {
    fn default() -> Self {
        Self::new()
    }
} 