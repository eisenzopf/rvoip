/// Session adapter for session-core integration
pub struct SessionAdapter {
    // TODO: Implement session-core session adapter
}

impl SessionAdapter {
    pub fn new() -> Self {
        Self {}
    }
}

impl Default for SessionAdapter {
    fn default() -> Self {
        Self::new()
    }
} 