/// Events adapter for session-core event integration
pub struct EventAdapter {
    // TODO: Implement session-core event adapter
}

impl EventAdapter {
    pub fn new() -> Self {
        Self {}
    }
}

impl Default for EventAdapter {
    fn default() -> Self {
        Self::new()
    }
} 