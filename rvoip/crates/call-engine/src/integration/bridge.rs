/// Bridge adapter for session-core bridge integration
pub struct BridgeAdapter {
    // TODO: Implement session-core bridge adapter
}

impl BridgeAdapter {
    pub fn new() -> Self {
        Self {}
    }
}

impl Default for BridgeAdapter {
    fn default() -> Self {
        Self::new()
    }
} 