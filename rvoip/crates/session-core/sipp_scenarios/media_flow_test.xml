<?xml version="1.0" encoding="ISO-8859-1" ?>
<!DOCTYPE scenario SYSTEM "sipp.dtd">

<!-- Media Flow Test Scenario for Session-Core Testing -->
<!-- This scenario tests actual RTP media exchange, not just SIP signaling -->

<scenario name="Media Flow Test">
  
  <!-- Send INVITE with SDP offering RTP media -->
  <send retrans="500">
    <![CDATA[
      INVITE sip:mediatest@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: sipp <sip:sipp@[local_ip]:[local_port]>;tag=[pid]SIPpTag00[call_number]
      To: mediatest <sip:mediatest@[remote_ip]:[remote_port]>
      Call-ID: [call_id]
      CSeq: 1 INVITE
      Contact: sip:sipp@[local_ip]:[local_port]
      Max-Forwards: 70
      Subject: Media Flow Test
      Content-Type: application/sdp
      Content-Length: [len]

      v=0
      o=user1 53655765 2353687637 IN IP[local_ip_type] [local_ip]
      s=-
      c=IN IP[local_ip_type] [local_ip]
      t=0 0
      m=audio [auto_media_port] RTP/AVP 0 8
      a=rtpmap:0 PCMU/8000
      a=rtpmap:8 PCMA/8000
      a=sendrecv
    ]]>
  </send>

  <!-- Expect 100 Trying (optional) -->
  <recv response="100" optional="true">
  </recv>

  <!-- Expect 180 Ringing (optional) -->
  <recv response="180" optional="true">
  </recv>

  <!-- Expect 200 OK with SDP answer -->
  <recv response="200" rtd="true" rrs="true">
    <action>
      <ereg regexp="&lt;([^&gt;]+)&gt;" search_in="hdr" header="Contact" assign_to="contact_uri"/>
      <!-- Extract remote RTP port from SDP -->
      <ereg regexp="m=audio ([0-9]+)" search_in="body" assign_to="remote_rtp_port"/>
      <!-- Extract remote IP from SDP connection line -->
      <ereg regexp="c=IN IP4 ([0-9.]+)" search_in="body" assign_to="remote_rtp_ip"/>
    </action>
  </recv>

  <!-- Send ACK -->
  <send>
    <![CDATA[
      ACK [$contact_uri] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: sipp <sip:sipp@[local_ip]:[local_port]>;tag=[pid]SIPpTag00[call_number]
      To: mediatest <sip:mediatest@[remote_ip]:[remote_port]>[peer_tag_param]
      Call-ID: [call_id]
      CSeq: 1 ACK
      Contact: sip:sipp@[local_ip]:[local_port]
      Max-Forwards: 70
      Content-Length: 0
    ]]>
  </send>

  <!-- 
    RTP MEDIA EXCHANGE PERIOD
    During this time, SIPp will:
    - Send RTP packets to [$remote_rtp_ip]:[$remote_rtp_port]
    - Listen for RTP packets on [auto_media_port]
    - Validate media flow statistics
  -->
  
  <!-- Start RTP stream and play media for 5 seconds -->
  <nop>
    <action>
      <!-- This is where RTP media exchange happens -->
      <!-- SIPp can play audio files and record received audio -->
    </action>
  </nop>
  
  <!-- Media exchange period - 5 seconds of active RTP -->
  <pause milliseconds="5000"/>
  
  <!-- Send re-INVITE to test media parameter changes -->
  <send retrans="500">
    <![CDATA[
      INVITE [$contact_uri] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: sipp <sip:sipp@[local_ip]:[local_port]>;tag=[pid]SIPpTag00[call_number]
      To: mediatest <sip:mediatest@[remote_ip]:[remote_port]>[peer_tag_param]
      Call-ID: [call_id]
      CSeq: 2 INVITE
      Contact: sip:sipp@[local_ip]:[local_port]
      Max-Forwards: 70
      Subject: Media Parameter Change
      Content-Type: application/sdp
      Content-Length: [len]

      v=0
      o=user1 53655765 2353687638 IN IP[local_ip_type] [local_ip]
      s=-
      c=IN IP[local_ip_type] [local_ip]
      t=0 0
      m=audio [auto_media_port] RTP/AVP 8 0
      a=rtpmap:8 PCMA/8000
      a=rtpmap:0 PCMU/8000
      a=sendrecv
    ]]>
  </send>

  <!-- Expect 200 OK for re-INVITE -->
  <recv response="200">
  </recv>

  <!-- Send ACK for re-INVITE -->
  <send>
    <![CDATA[
      ACK [$contact_uri] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: sipp <sip:sipp@[local_ip]:[local_port]>;tag=[pid]SIPpTag00[call_number]
      To: mediatest <sip:mediatest@[remote_ip]:[remote_port]>[peer_tag_param]
      Call-ID: [call_id]
      CSeq: 2 ACK
      Contact: sip:sipp@[local_ip]:[local_port]
      Max-Forwards: 70
      Content-Length: 0
    ]]>
  </send>

  <!-- Continue media exchange with new codec for 3 seconds -->
  <pause milliseconds="3000"/>

  <!-- Send BYE to terminate the call -->
  <send retrans="500">
    <![CDATA[
      BYE [$contact_uri] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: sipp <sip:sipp@[local_ip]:[local_port]>;tag=[pid]SIPpTag00[call_number]
      To: mediatest <sip:mediatest@[remote_ip]:[remote_port]>[peer_tag_param]
      Call-ID: [call_id]
      CSeq: 3 BYE
      Contact: sip:sipp@[local_ip]:[local_port]
      Max-Forwards: 70
      Content-Length: 0
    ]]>
  </send>

  <!-- Expect 200 OK for BYE -->
  <recv response="200" crlf="true">
  </recv>

  <!-- Media flow test completed successfully -->
  
</scenario> 