<?xml version="1.0" encoding="ISO-8859-1" ?>
<!DOCTYPE scenario SYSTEM "sipp.dtd">

<!-- 
🎵 UAC Audio Streaming Test Scenario
=====================================

This scenario makes a SIP call and streams PCMA audio to test rtp_stream functionality.

Usage: 
  sipp -sf uac_audio_test.xml -mi 127.0.0.1 -mp 7000 127.0.0.1:5080
-->

<scenario name="UAC Audio Streaming Test">
  
  <!-- Send INVITE with SDP audio offer -->
  <send retrans="500">
    <![CDATA[
      INVITE sip:testuser@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/UDP [local_ip]:[local_port];branch=z9hG4bK-[pid]-[call_number]
      From: Audio Client <sip:client@[local_ip]:[local_port]>;tag=[pid]SIPpTag[call_number]
      To: Audio Server <sip:testuser@[remote_ip]:[remote_port]>
      Call-ID: [call_id]
      CSeq: [cseq] INVITE
      Contact: <sip:client@[local_ip]:[local_port]>
      Max-Forwards: 70
      Subject: SIPp Audio Streaming Test
      Content-Type: application/sdp
      Content-Length: [len]

      v=0
      o=client 53655765 2353687637 IN IP4 [local_ip]
      s=SIPp Audio Test
      c=IN IP4 [media_ip]
      t=0 0
      m=audio [auto_media_port] RTP/AVP 0 8
      a=rtpmap:0 PCMU/8000
      a=rtpmap:8 PCMA/8000
      a=sendrecv
    ]]>
  </send>

  <!-- Expect 100 Trying (optional) -->
  <recv response="100" optional="true">
  </recv>

  <!-- Expect 180 Ringing (optional) -->
  <recv response="180" optional="true">
  </recv>

  <!-- Expect 200 OK -->
  <recv response="200" rtd="true">
  </recv>

  <!-- Send ACK -->
  <send>
    <![CDATA[
      ACK sip:testuser@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/UDP [local_ip]:[local_port];branch=z9hG4bK-[pid]-[call_number]-ack
      From: Audio Client <sip:client@[local_ip]:[local_port]>;tag=[pid]SIPpTag[call_number]
      To: Audio Server <sip:testuser@[remote_ip]:[remote_port]>[peer_tag_param]
      Call-ID: [call_id]
      CSeq: [cseq] ACK
      Contact: <sip:client@[local_ip]:[local_port]>
      Max-Forwards: 70
      Content-Length: 0
    ]]>
  </send>

  <!-- Log call establishment -->
  <nop>
    <action>
      <log message="🎵 UAC: Call established, starting audio streaming..."/>
    </action>
  </nop>

  <!-- Wait a moment for media session to establish -->
  <pause milliseconds="500"/>

  <!-- START STREAMING AUDIO VIA PCAP -->
  <nop>
    <action>
      <exec play_pcap_audio="../../pcap/g711a.pcap"/>
      <log message="🎵 UAC: Started playing G.711A RTP stream from PCAP"/>
    </action>
  </nop>

  <!-- Let PCAP play for its duration (1 second) + extra time -->
  <pause milliseconds="2000"/>

  <!-- Log completion -->
  <nop>
    <action>
      <log message="🎵 UAC: PCAP audio playback completed"/>
    </action>
  </nop>

  <!-- Wait a bit more -->
  <pause milliseconds="1000"/>

  <!-- Send BYE to end call -->
  <send retrans="500">
    <![CDATA[
      BYE sip:testuser@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/UDP [local_ip]:[local_port];branch=z9hG4bK-[pid]-[call_number]-bye
      From: Audio Client <sip:client@[local_ip]:[local_port]>;tag=[pid]SIPpTag[call_number]
      To: Audio Server <sip:testuser@[remote_ip]:[remote_port]>[peer_tag_param]
      Call-ID: [call_id]
      CSeq: [cseq] BYE
      Contact: <sip:client@[local_ip]:[local_port]>
      Max-Forwards: 70
      Content-Length: 0
    ]]>
  </send>

  <!-- Expect 200 OK for BYE -->
  <recv response="200" crlf="true">
  </recv>

  <!-- Log completion -->
  <nop>
    <action>
      <log message="✅ UAC: Audio streaming test completed"/>
    </action>
  </nop>
  
</scenario> 