# General-Purpose SIP Client Roadmap

Tracking doc for the remaining blockers between today's `session-core`
(signalling-complete, RFC 3261 §14.1 hold/resume correct, NOTIFY +
RFC 3515 §2.4.5 wired) and a credible "general-purpose SIP client" that
can connect to Asterisk, FreeSWITCH, and cloud carriers (Twilio,
Vonage, Bandwidth) end-to-end.

This is the **next document after** `PRE_B2BUA_ROADMAP.md`. The
b2bua/IVR/recording API surface (per-call event streams, bridge
primitive, NOTIFY) has all landed. What remains is everything outside
the call-control story: transport (TLS/TCP/SRV/NAT), media security
(SRTP), and trunking headers (PAI, Service-Route).

Cross-references:
- `RFC_COMPLIANCE_STATUS.md` — current method/header matrix.
- `PRE_B2BUA_ROADMAP.md` — multi-session control plane (closed).
- `TELCO_USE_CASE_ANALYSIS.md` — softphone / B2BUA / IVR / PBX / E911 use cases.

---

## Reality check by deployment target

| Target | Today | What still blocks it |
|--------|-------|----------------------|
| **Asterisk / FreeSWITCH on LAN, IP-based, PCMU/PCMA** | ✅ works | nothing |
| **Asterisk / FreeSWITCH with `srtp` enabled** (default in modern installs) | ✅ | RFC 4568 SDES-SRTP fully wired — `Config::offer_srtp = true` |
| **PBX trunking with carrier-presented identity** | ✅ | P-Asserted-Identity (RFC 3325) end-to-end via `Config::pai_uri` / `make_call_with_pai` (B1) |
| **Cloud carriers — Twilio, Vonage, Bandwidth (TLS + SRTP + PAI)** | ✅ | TLS (A1), SDES-SRTP (B2), PAI (B1) all shipped — `Config::tls_*` + `offer_srtp=true` + `pai_uri` |
| **Carriers requiring TLS 5061** | ✅ | A1 — `Config::tls_cert_path` + `tls_key_path` + auto-bind 5060→5061 + URI-aware multiplexer routes `sips:` |
| **UAC behind NAT, long-duration registration** | ✅ | A3 — RFC 3581 NAT discovery + RFC 5626 §5 Contact rewrite. Long-duration NAT-traversal via RFC 5626 §3.5.1 CRLFCRLF keep-alive on TCP/TLS (A5 Phase 2b-min) plus stateful `OutboundFlow` + failure-driven re-REGISTER on pong timeout / connection close (A5 Phase 2c, 1s debounce per AoR). |
| **Carrier with geo-failover via `_sip._udp` SRV** | ✅ | A4 — RFC 3263 §4 ladder (IP literal → A/AAAA+explicit port → `_service._proto.host` SRV with RFC 2782 weighted selection → A/AAAA fallback); NAPTR deferred. |
| **WebRTC peer (browser ↔ session-core)** | ❌ | WebSocket client transport (RFC 7118), DTLS-SRTP SDP, ICE/TURN — Tier D, future |
| **PBX requiring TCP for large SDP / video** | ✅ | A2 — TCP enable flag flipped + URI-aware selection routes `;transport=tcp` + RFC 3261 §18.1.1 MTU fallback (when used) |

---

## Source-of-truth findings

Audit of the workspace as of 2026-04-20.

### Transport — `crates/sip-transport`

- **TCP**: production-ready in `crates/sip-transport/src/transport/tcp/mod.rs` (255 LOC, server + client pool, RFC 3261 §18.3 framing, 300 s idle keep-alive). Just gated off by `enable_tcp: false` at `crates/session-core/src/api/unified.rs:822`.
- **TLS**: two conflicting impls.
  - `crates/sip-transport/src/tls.rs` — server-side complete (rustls), **client `connect()` returns `NotImplemented` at line 254**.
  - `crates/sip-transport/src/transport/tls/mod.rs` — placeholder; `send_message()` returns `NotImplemented` at line 66.
  - dialog-core's `add_tcp_transport(addr, tls=true)` at `transaction/transport/mod.rs:256` explicitly rejects TLS, so even flipping the flag wouldn't help today.
- **WebSocket (RFC 7118)**: server complete in `crates/sip-transport/src/transport/ws/mod.rs` (subprotocol negotiation, WSS, accept loop). **Client `connect_to()` stubbed at line 229.** Feature-gated `ws` is on by default in Cargo.toml.
- session-core hardcodes `enable_tls: false`, `enable_tcp: false`, `enable_ws: false`. `Config` exposes no `tls_cert_path`, `tls_key_path`, or transport-preference fields.

### Media & RTP — `crates/media-core` + `crates/rtp-core`

- **Codecs registered**: PCMU (PT 0), PCMA (PT 8), G.722 (PT 9), G.729 (PT 18), Opus (PT 96). Telephone-event (PT 101) constant defined; **RTP DTMF event encode/decode not wired** (`crates/media-core/src/codec/audio/mod.rs:26`).
- **SRTP crypto core present** at `crates/rtp-core/src/srtp/` (AES-CM, AES-F8, HMAC-SHA1).
- **DTLS-SRTP framework present** at `crates/rtp-core/src/dtls/` (RFC 5764 key extraction, role negotiation; depends on `webrtc-dtls 0.9`).
- **SDES-SRTP client** stubbed at `crates/rtp-core/src/api/client/security/srtp/sdes.rs` but **no `a=crypto:` SDP builder** — Asterisk and FreeSWITCH cannot negotiate encrypted media with us today even though the crypto primitives are in the workspace.
- **SDP parser** recognises `a=crypto:`, `a=fingerprint:`, `a=setup:`, `a=ice-ufrag:`, `a=candidate:` (`crates/sip-core/src/sdp/parser/attribute_parser.rs`). **None of those have builders** in `crates/sip-core/src/builder/`.
- **SDP offer/answer matching** is a TODO stub at `crates/dialog-core/src/sdp/negotiation.rs:31` — current flows work because session-core handles negotiation in its own actions.
- **STUN / ICE / TURN**: parser sees `a=ice-ufrag:` / `a=candidate:`, no agent or candidate gatherer exists.
- **RFC 3389 Comfort Noise (PT 13)**: not implemented. VAD exists at `crates/media-core/src/processing/audio/vad.rs` but no CN packet generation.
- **Video**: VP8 (PT 97) / VP9 (PT 98) RTP payload handlers exist. No H.264. No `m=video` builder. No video transmitter wiring in media-core.

### SIP headers — `crates/sip-core` + `crates/dialog-core`

Implemented (verified):
- Record-Route / Route + loose routing (`crates/dialog-core/src/dialog/dialog_impl.rs:58-59`).
- `Path` header type + builder (`crates/sip-core/src/types/path.rs`).
- `Max-Forwards` defaults to 70 (`crates/dialog-core/src/transaction/client/builders.rs:79`).
- `Supported`, `Allow`, `Require`, `Unsupported`.
- `Reason` header (RFC 3326) — landed in the session-timer-failure work.
- Multipart/mixed bodies.
- IPv6 in transport + Via.

Absent (not implemented anywhere in the workspace):
- **P-Asserted-Identity / P-Preferred-Identity** (RFC 3325) — *load-bearing for SIP trunking*.
- **Service-Route** (RFC 3608) — registrar-returned route set; RFC 5626 SIP Outbound depends on it.
- **GRUU** (RFC 5627) — `+sip.instance` / `pub-gruu` / `temp-gruu`.
- **History-Info** (RFC 7044) and **Diversion** (RFC 4244).
- **Privacy** (RFC 3323).
- **Compact header forms (RX)** (RFC 3261 §7.3.3) — `f=`/`t=`/`v=`/`m=`/etc. Parser doesn't expand them.
- **Outbound proxy** static route knob.
- **PUBLISH** (RFC 3903) — silent-fallthrough stub already documented.
- **OPTIONS outbound helper** for keep-alive.

---

## Tiered backlog

Each tier is independently shippable. Tier letter is value-per-effort, not severity.

### Tier A — Carrier transport

Already enumerated in `PRE_B2BUA_ROADMAP.md` as P1–P7. This audit confirms each against the source.

| # | Item | RFC | Status | Files |
|---|------|-----|--------|-------|
| A1 | TLS client connector + config wiring | RFC 5630, RFC 3261 §26 | ✅ | `sip-transport/src/transport/tls/mod.rs` (consolidated, ~600 LOC); `tokio_rustls::TlsConnector` with `rustls-native-certs` + `webpki-roots` fallback + optional extra CA + dev-only insecure-skip; `dialog-core/src/transaction/transport/mod.rs` enables TLS via `add_tcp_transport(addr, true)` with `bind_addr.port() + 1` (RFC 3261 5060→5061 convention); `session-core/src/api/unified.rs::Config` carries `tls_cert_path` / `tls_key_path` / `tls_extra_ca_path` / `tls_insecure_skip_verify`; `MultiplexedTransport::pick_transport` routes Responses via `Transport::has_connection_to(addr)` (RFC 3261 §17.2 / §18.2.2). Tests: `crates/sip-transport/tests/tls_handshake_test.rs` + `crates/session-core/tests/tls_call_integration.rs`. See `crates/TLS_SIP_IMPLEMENTATION_PLAN.md`. |
| A2 | TCP enable-flag wiring + URI-aware transport selection | RFC 3261 §18.3, §19.1.5, §26.2 | ✅ | `MultiplexedTransport` (`crates/dialog-core/src/transaction/transport/multiplexed.rs`) wraps the per-flavour registry; `TransactionManager::with_transport_manager` installs it as the single `Arc<dyn Transport>`, routing outbound by URI scheme + `;transport=` parameter; `enable_tcp: true` flipped at `crates/session-core/src/api/unified.rs`. `resolve_uri_to_socketaddr` uses typed `Uri` fields + `sips:`→5061 default + async DNS. 10 multiplexer unit tests (6 selection + 4 dispatch). |
| A3 | RFC 3581 NAT discovery + RFC 5626 §5 Contact rewrite | RFC 3581, RFC 5626 §5 | ✅ | `DialogManager::nat_discovered_addr` cache populated by `ResponseHandler::handle_response_message` from typed `Param::Received` + `Param::Rport(Some(_))` on every inbound response (no-op suppression when discovered == local); `UnifiedDialogApi::discovered_public_addr()` exposes the cache; `DialogAdapter::send_register` pre-rewrites Contact host:port via `rewrite_contact_host` helper (preserves scheme + user-part + URI params). 5 NAT-discovery extraction tests + 5 Contact-rewrite tests. |
| A4 | RFC 3263 SRV + A/AAAA resolution (NAPTR deferred) | RFC 3263 | ✅ core | `hickory-resolver 0.24` added to workspace + dialog-core; new module `crates/dialog-core/src/dialog/dns_resolver.rs` with `resolve_uri(uri) → ResolvedTarget { addr, transport }` implementing RFC 3263 §4 ladder (IP literal → A/AAAA-with-explicit-port → `_service._proto.host` SRV with RFC 2782 weighted selection → A/AAAA fallback); scheme-aware SRV service labels (`sip:`→`_sip._udp`/`_sip._tcp`, `sips:`→`_sips._tcp`, `ws`/`wss` for RFC 7118). Back-compat wrapper `resolve_uri_to_socketaddr` preserved at `crates/dialog-core/src/dialog/dialog_utils.rs`. `crates/dialog-core/src/transaction/manager/handlers.rs::resolve_uri_to_socketaddr` (ACK destination helper) redirected to the shared resolver. 15 unit tests covering scheme-to-default-port, SRV service-name derivation, RFC 2782 weighted selection (zero-weight, weighted, priority gating), and IP-literal short-circuit paths. NAPTR (RFC 3263 §4.1) deferred — its incremental value is narrow once `;transport=` and scheme-based selection already work; add if a deployment needs it. |
| A5 | SIP Outbound — REGISTER Contact rewrite end-to-end + CRLFCRLF keep-alive + stateful OutboundFlow + failure-driven re-REGISTER | RFC 5626 | ✅ Phases 1 + 2a + 2b-min + 2c | **Phase 1**: `crates/sip-core/src/types/outbound.rs` — `OutboundContactParams { instance_urn, reg_id }` + `set/read_outbound_contact_params(Address)` helpers with RFC 5626 §4.1 quoting (`+sip.instance="<urn:...>"`) and §4.2 reg-id. `mark_uri_as_outbound(Address)` adds the §5.4 `;ob` URI parameter (idempotent). 10 unit tests. **Phase 2a** (2026-04-23): session-core `Config::sip_outbound_enabled` / `sip_instance` / `outbound_keepalive_interval_secs` knobs parsed at `UnifiedCoordinator` boot; `DialogAdapter::outbound_contact_params` field populated when enabled (only if a stable `sip_instance` URN is supplied — otherwise a warning is logged and fallback to pre-5626 REGISTER Contact is used). New `UnifiedDialogApi::send_register_with_outbound_contact` dialog-core API built on a pure `build_outbound_contact(uri, params)` helper that produces a Contact with `<uri;ob>;+sip.instance="<urn>";reg-id=N`; legacy `send_register` preserved. `DialogAdapter::send_register` routes through the outbound variant when `outbound_contact_params` is set, else the legacy path. 4 dialog-core unit tests (`builds_contact_with_instance_regid_and_ob_flag`, `ob_flag_goes_on_uri_params_section`, `invalid_uri_returns_error`, `reg_id_value_propagates`). **Phase 2b-min** (2026-04-23): RFC 5626 §3.5.1 CRLFCRLF keep-alive — *stateless* ping shape. `Transport::send_raw(dest, Bytes)` + `TransportEvent::KeepAlivePongReceived` + `TransportEvent::ConnectionClosed` added to the trait; TCP/TLS read loops recognise leading `\r\n`/`\r\n\r\n` at buffer offset 0 as pong/ping frames (consumed before the SIP parser sees them; server auto-answers pings with CRLF pong); TCP stream split into owned read/write halves via `TcpStream::into_split` so reads and writes never contend; `ConnectionClosed` emitted before pool/registry eviction; `has_connection_to` implemented on TCP (fixes a latent multiplexer response-routing bug). `MultiplexedTransport::send_raw` probes connection-oriented transports via `has_connection_to` and dispatches. `DialogManager::start_outbound_ping((aor, reg_id, instance), dest)` spawns a `tokio::time::interval` task that writes CRLFCRLF at the configured interval and silently exits on send failure; `outbound_keepalive_interval_secs` from session-core Config is now threaded into `DialogManager` at coordinator boot. `record_outbound_flow_from_response` in `protocol/response_handler.rs` re-parses REGISTER 2xx Contact for `+sip.instance`/`reg-id`/`;ob`, pulls the destination from `TransactionManager::transaction_destination(tx_id)` (new accessor), and calls `start_outbound_ping`. 15 new unit + transport-integration tests across sip-transport/dialog-core. **Phase 2c (pending)**: upgrade the stateless ping into a stateful `OutboundFlow` with pong-timeout tracking and `RegistrationFlowFailed` event wired into session-core's `EventType::RefreshRegistration` with ~1 s debounce. |
| A6 | STUN client + `Config::media_public_addr` / `stun_server` | RFC 8489 | ✅ | `crates/rtp-core/src/network/stun/{mod,message}.rs` (hand-rolled ~250 LOC RFC 8489 codec); `StunClient` shares the bound RTP socket (cone-NAT mapping invariant); `crates/session-core/src/api/unified.rs::Config::media_public_addr` (static override) + `Config::stun_server` (boot-time probe, 1500 ms budget, soft-fail); `MediaAdapter::public_rtp_addr` injects discovered/configured public IP+port into offer + answer SDP `c=`/`o=`/`m=audio`. 10 codec unit tests + 3 loopback integration. STUN auth (long-term credential, MESSAGE-INTEGRITY) deferred to Sprint 4 D3 (ICE prerequisite). |
| A7 | Digest enhancements: `nc` counter tracking, `auth-int` qop, `-sess` algorithms | RFC 7616 / RFC 8760 | ✅ | `crates/auth-core/src/sip_digest.rs` rewritten — `DigestAlgorithm` extended with `MD5Sess`/`SHA256Sess`, `sha2` dep added, algorithm-aware hash dispatch, additive `DigestClient::compute_response_with_state(nc, body)` + `format_authorization_with_state` (legacy `compute_response` preserved); `SessionState::digest_nc: HashMap<(String, String), u32>` keyed by realm+nonce; `SendINVITEWithAuth` and `dialog_adapter::send_register` increment via `entry().and_modify(|n| *n += 1).or_insert(1)`; INVITE folds local SDP body into HA2 when challenge offers `auth-int`. SHA-512-256 + RFC 8760 deferred (rare in deployments). 13 auth-core lib tests (was 5, +8 new). |

### Tier B — PBX trunking essentials (NEW — not in prior docs)

These bite the moment session-core points at a real Asterisk/FreeSWITCH **trunk** (vs LAN extension) or any cloud SIP trunk.

| # | Item | RFC | Status | Files |
|---|------|-----|--------|-------|
| B1 | P-Asserted-Identity / P-Preferred-Identity | RFC 3325 | ✅ end-to-end | sip-core types + parser + builder ext ✅, dialog-core `make_call_with_extra_headers[_for_session]` + `send_initial_invite_with_extra_headers` ✅, session-core `Config::pai_uri` + `UnifiedCoordinator::make_call_with_pai` + `SessionState.pai_uri` + `Action::SendINVITE` routing ✅, inbound PAI on `IncomingCallInfo.p_asserted_identity` ✅. Wire-level multi-binary integration test still TODO (call this `tests/pai_integration.rs`). |
| B2 | SDES-SRTP — full end-to-end (SDP builder + parser + key exchange + transport encrypt/decrypt) | RFC 4568, RFC 3711 | ✅ | sip-core `CryptoSuite` + `CryptoAttribute` types/parser/builder; session-core `SrtpNegotiator` + `Config::offer_srtp` / `srtp_required` / `srtp_offered_suites`; rtp-core `UdpRtpTransport::set_srtp_contexts` wraps send/receive; media-core `install_srtp_contexts`. See `crates/STEP_2B_SRTP_INTEGRATION_PLAN.md` for the four-phase landing log. |
| B3 | Service-Route processing on REGISTER 200 | RFC 3608 | ✅ core + cache | sip-core: new `ServiceRoute` type + parser + builder at `crates/sip-core/src/types/service_route.rs` / `crates/sip-core/src/builder/headers/service_route.rs`; `HeaderName::ServiceRoute` + `TypedHeader::ServiceRoute` wired across `header_name.rs` / `typed_header.rs` / `request.rs` / `response.rs`. dialog-core: `DialogManager::service_route_by_aor` cache (same pattern as A3 `nat_discovered_addr`); `extract_service_route` + `record_service_route_from_response` in `crates/dialog-core/src/protocol/response_handler.rs` called from `handle_response_message` on every 2xx REGISTER response (CSeq-method gated); exposed via `UnifiedDialogApi::service_route_for_aor(aor)`. **Preload** of cached Service-Route into outbound dialog `route_set` is a follow-up (consumer: session-core media adapter / dialog creation path) — the cache is the primitive that unblocks it. |
| B4 | DTMF RTP events — codec + RX dispatch + TX API + SDP attribute advertisement | RFC 4733 | ✅ | **Codec** (`crates/media-core/src/codec/audio/dtmf.rs`): `DtmfEvent` + `TelephoneEvent` types implementing RFC 4733 §2.3 4-byte wire format (event/E/R/volume/duration); encode+decode round-trip; forward-compat tolerance on oversized payloads; 10 unit tests. **RX**: `RtpEvent::DtmfEvent` variant on `rtp-core/src/traits/mod.rs`; PT 101 inline decode in `rtp-core/src/transport/udp.rs` surfacing typed fields instead of generic `MediaReceived`; `RtpSessionEvent::DtmfReceived` in `rtp-core/src/session/mod.rs` for downstream consumers. media-core: new `DtmfNotification` + `dtmf_callbacks: DashMap<DialogId, mpsc::Sender<_>>` on `MediaSessionController`, with deduplication of the three §2.5.1.3 retransmits (fire once per digit on first E=1 frame). session-core `MediaAdapter::install_dtmf_callback` registers the callback per session and republishes as `Event::DtmfReceived` on the API event bus via `GlobalEventCoordinator`. **TX**: new `RtpSession::send_packet_with_pt` overrides the configured audio PT so `MediaSessionController::send_dtmf_packet` can transmit a PT 101 telephone-event; `MediaAdapter::send_dtmf` (stub) and new `send_dtmf_rfc4733` (session-id + duration) wire the pre-existing `UnifiedCoordinator::send_dtmf` through to real wire transmission. **SDP**: `a=rtpmap:101 telephone-event/8000` + `a=fmtp:101 0-15` emitted on both offer and answer paths in `session-core/src/adapters/media_adapter.rs` whenever PCMU/PCMA is offered (already present from the codec landing). **Tests**: 1 rtp-core PT 101 dispatch integration test; `audio_roundtrip_integration` remains green. |
| B5 | Compact header forms on receive | RFC 3261 §7.3.3 + IANA later-RFC additions | ✅ | `sip-core/src/types/headers/header_name.rs:232-305` — `FromStr` maps all RFC 3261 compact forms (`i/m/e/l/c/f/s/k/t/v`) plus later-RFC additions (`o` Event, `u` Allow-Events, `r` Refer-To, `b` Referred-By, `x` Session-Expires). Case-insensitive. Parser dispatch goes through this by default. |

### Tier C — Media-path completeness (NEW)

QoS items that don't break interop in lab tests but matter in production.

| # | Item | RFC | Status | Files |
|---|------|-----|--------|-------|
| C1 | Comfort Noise (PT 13) | RFC 3389 | ✅ primitives + SDP advertisement | `crates/media-core/src/codec/audio/mod.rs` — PT 13 codec constant added; new `crates/media-core/src/relay/controller/cn_transmitter.rs` (~150 LOC, mirrors `dtmf_transmitter.rs` shape) emits one PT 13 packet per `send(level)` call carrying RFC 3389 §3.1 one-byte payload (level clamped to 0..=127); `Config::comfort_noise_enabled` on `crates/session-core/src/api/unified.rs::Config` propagates to `MediaAdapter::comfort_noise_enabled`; offer + answer SDP advertise `13` between PCMA and telephone-event with `a=rtpmap:13 CN/8000` when enabled. 2 transmitter unit tests + 2 SDP-shape unit tests. **Follow-up**: VAD-driven gating of the audio transmit loop (transmitter primitive shipped; callers can drive CN from their own VAD; loop refactor is a separate workstream). |
| C2 | SDP offer/answer matching helper | RFC 3264 | ✅ matcher + consumer | `crates/dialog-core/src/sdp/negotiation.rs` rewritten from `// TODO` stub to ~340-LOC RFC 3264 §6 matcher. Public surface: `match_offer(offer, &AnswerCapabilities) → OfferAnswerMatch`. Per-`m=`-line accept/reject; intersection in offerer-preference order (§6 ¶ 3); `accept_srtp` / `require_srtp` policy gates short-circuit before format intersection; `rtpmap`/`fmtp`/`rtcp-fb` filtered to surviving formats; ice-ufrag/ice-pwd/fingerprint/setup/candidate/mid/ssrc/group/msid/extmap carried through verbatim so Sprint 4 D2/D3/D5 consumers extend the matcher rather than add a third hardcoded SDP shape. 11 integration tests in `crates/session-core/tests/sdp_matcher_integration.rs`. **Sprint 3.5 swap**: `negotiate_sdp_as_uas` swapped over via the pure `compute_answer_formats` helper that takes `(offer, comfort_noise_enabled, strict, offer_srtp, srtp_required) → Vec<String>`; new `Config::strict_codec_matching` (default `true`) — RFC-correct out of the box, set to `false` to recover the pre-Sprint-3.5 permissive "always full set" behavior. 4 helper unit tests covering strict intersection, permissive fall-through, no-overlap-fails-with-488-shape, and CN-preserved-in-intersection. |

### Tier D — WebRTC interop (NEW; only if browser interop is in scope)

| # | Item | RFC | Status | Files |
|---|------|-----|--------|-------|
| D1 | WebSocket client transport | RFC 7118 | ⬜ | `sip-transport/src/transport/ws/mod.rs:229` |
| D2 | DTLS-SRTP SDP builder (`a=fingerprint:`, `a=setup:`) | RFC 5763, RFC 5764 | ⬜ | new `sip-core/src/sdp/builder/dtls.rs`; consume existing `rtp-core/src/dtls/` |
| D3 | ICE agent + candidate gathering | RFC 8445 | ⬜ | new `rtp-core/src/ice/` |
| D4 | TURN client | RFC 8656 | ⬜ | new `rtp-core/src/turn/` |
| D5 | H.264 RTP payload + `m=video` builder | RFC 6184 | ⬜ | `rtp-core/src/payload/h264.rs`; SDP builder |

### Tier E — Aesthetic / advanced

| # | Item | RFC | Status |
|---|------|-----|--------|
| E1 | GRUU — Contact `pub-gruu`/`temp-gruu` helpers landed | RFC 5627 | ✅ primitives | Same module as A5 Phase 1 (`crates/sip-core/src/types/outbound.rs`): `GruuContactParams { pub_gruu, temp_gruu }` + `set/read_gruu_contact_params(Address)` helpers with proper quoting. 3 unit tests (write+read, pub-only, clearing). Registrars that return GRUU on REGISTER 2xx can now be parsed; UAs can advertise GRUU to peers via Contact. **Follow-up**: extraction on REGISTER 2xx response side (same shape as B3 Service-Route — a `DialogManager::gruu_by_aor` cache + `UnifiedDialogApi::gruu_for_aor()` accessor). |
| E2 | History-Info / Diversion | RFC 7044 / RFC 4244 | ⬜ |
| E3 | Privacy header | RFC 3323 | ⬜ |
| E4 | Outbound proxy static route knob | RFC 3261 §8.1.2 | ✅ INVITE path | `Config::outbound_proxy_uri: Option<String>` in `crates/session-core/src/api/unified.rs`, parsed to `Uri` at `UnifiedCoordinator` construction (malformed URI fails loudly). `DialogAdapter::outbound_proxy_uri` stored; `prepend_outbound_proxy_route` pure helper prepends `Route: <proxy;lr>` to the extras list when set. Wired through `SendINVITE` action so every outgoing INVITE (with or without PAI) carries the pre-loaded Route. REGISTER not yet routed through this proxy — deferred to A5 (RFC 5626 Outbound) where flow-token mechanics land. 3 unit tests. |
| E5 | 305 Use Proxy / 380 Alternative Service proxy semantics (reason surfacing) | RFC 3261 §21.3 | ✅ minimal | New `format_redirect_or_terminate_reason(response)` in `crates/dialog-core/src/protocol/response_handler.rs` — for redirect status codes (300/301/302/305/380) appends alternate Contact URIs to the `CallTerminated` event's `reason` string (e.g. `"305 Use Proxy (alternate: sip:proxy.example.com;lr)"`). Non-redirect 3xx/4xx/5xx/6xx responses unchanged. Application layer can parse the reason and retry via `make_call(alternate)`. 5 unit tests. **Follow-up**: fully-typed `SessionCoordinationEvent::CallRedirected { alternate_targets: Vec<Uri> }` + auto-retry policy deferred until a deployment needs it. |
| E6 | PUBLISH — wire through dialog-core or remove the silent-fallthrough YAML stub | RFC 3903 | ⬜ |
| E7 | Outbound OPTIONS helper (superseded by RFC 5626 keep-alive) | RFC 3261 §11 | ⬜ |

---

## Sprint plan

| Sprint | Items | Outcome |
|--------|-------|---------|
| **1 — make it talk to real PBXs** ✅ | A1 (TLS) ✅ + A2 (TCP flip) ✅ + A3 (Contact rewrite) ✅ + B1 (P-Asserted-Identity) ✅ + B2 (SDES-SRTP SDP) ✅ | **Shipped 2026-04-22.** Asterisk/FreeSWITCH with `srtp` and trunk identity; Twilio/Vonage/Bandwidth on TLS+SRTP+PAI; UAs behind NAT register correctly via Contact rewrite. |
| **2 — production-NAT and registration robustness** ✅ | A4 (SRV/NAPTR) ✅ + A5 (RFC 5626 SIP Outbound — Phases 1/2a/2b-min/2c) ✅ + B3 (Service-Route) ✅ + B4 (RFC 4733 DTMF — codec + wiring) ✅ + B5 (compact headers RX) ✅ | **Shipped 2026-04-23.** Long-duration registrations through SBCs survive pong timeouts and connection drops (failure-driven re-REGISTER with 1s debounce); carrier geo-failover via SRV; in-band DTMF both directions. |
| **2.5 — architectural hygiene** ✅ | P1 (boundary rule) ✅ + P6 (TLS feature gate) ✅ + P5 (MediaSessionId alias only — `state_table::DialogId` preserved as Uuid newtype) ✅ + P2 (unified SDP + non-SRTP DTMF bug fix) ✅ + P4 (dedup relocation) ✅ + P3 (RFC 4733 §2.5.1.3 multi-packet send) ✅ | **Shipped 2026-04-25.** Dead state-machine variants deleted + strum-derived YAML drift detection; `tls_insecure_skip_verify` behind `dev-insecure-tls` Cargo feature; `MediaSessionId` aliased to `rvoip_media_core::DialogId` (eliminates `from_dialog` reconstruction); single `generate_local_sdp` entry point that always advertises PCMU + PCMA + telephone-event (fixes pre-existing plaintext DTMF gap); RFC 4733 retransmit dedup moved to rtp-core's UDP layer; outbound DTMF emits the full RFC 4733 §2.5.1.3 packet schedule. No user-visible feature changes. |
| **3 — media polish** ✅ | A7 (digest hardening — NC counter + auth-int + SHA-256/-sess) ✅ + A6 (STUN client + `Config::media_public_addr` / `stun_server`) ✅ + C1 (RFC 3389 Comfort Noise — SDP advertise + `CnTransmitter` primitive) ✅ + C2 (RFC 3264 §6 matcher in dialog-core) ✅ + Sprint 3.5 (consume the C2 matcher in `negotiate_sdp_as_uas`) ✅ | **Shipped 2026-04-25.** auth-core: per-(realm, nonce) NC counter via additive `compute_response_with_state` overload + new `format_authorization_with_state`; SHA-256 + SHA-256-sess + MD5-sess parsed and hashed correctly; `auth-int` qop selected when offered; legacy `compute_response` preserved as a wrapper. session-core threads NC through `SessionState::digest_nc`; INVITE auth folds the SDP body into HA2 when the carrier offers `auth-int`. rtp-core: hand-rolled RFC 8489 STUN codec at `crates/rtp-core/src/network/stun/`; `StunClient` shares the bound RTP socket; `Config::media_public_addr` + `Config::stun_server` thread into `MediaAdapter::public_rtp_addr` and override `c=`/`o=`/`m=audio` lines on offer + answer. media-core: PT 13 codec constant; new `cn_transmitter.rs` mirrors `dtmf_transmitter.rs` shape; `Config::comfort_noise_enabled` propagates to SDP advertisement (`13` + `a=rtpmap:13 CN/8000` on offer + answer). dialog-core: generic RFC 3264 §6 `match_offer()` matcher in `crates/dialog-core/src/sdp/negotiation.rs` (~340 LOC) with `AnswerCapabilities` + `OfferAnswerMatch` + per-`m=`-line accept/reject + offerer-order intersection + verbatim attribute carry-over (rtpmap/fmtp filtered to surviving formats; ice-ufrag/ice-pwd/fingerprint/setup pass through unchanged). **Scope cuts**: SHA-512-256 + STUN auth (long-term credential, MESSAGE-INTEGRITY) deferred (Sprint 4 ICE prerequisite); session-core's `negotiate_sdp_as_uas` not yet swapped over to the new matcher (matcher is ready, swap is non-trivial alongside SRTP plumbing — follow-up); VAD-driven CN transmit-loop gating is a follow-up (transmitter primitive landed, SDP advertisement landed). Plan: `~/.claude/plans/we-just-finished-implemnting-immutable-pizza.md`. |
| **4 — WebRTC (opt-in)** | D1–D5 in order | Browser ↔ session-core works |
| **Backlog** | Tier E | Pull individually when a deployment asks |

---

## Verification strategy (per item)

- **TLS / TCP flip**: extend `examples/streampeer/run_all.sh` with a TLS-enabled Bob; assert UDP-Alice ↔ TLS-Bob fails (config mismatch) and TLS-Alice ↔ TLS-Bob succeeds. Run `audio_roundtrip_integration` over TLS to confirm media still flows.
- **SDES-SRTP**: round-trip integration test mirroring `audio_roundtrip_integration` with `a=crypto:` in both SDPs; assert wire-captured RTP is encrypted (no readable PCMU header in payload).
- **P-Asserted-Identity**: unit test on the builder + multi-binary test asserting an inbound INVITE's PAI is surfaced on `IncomingCallInfo`.
- **RFC 3263 SRV**: in-process mock DNS resolver returning weighted SRV records; assert UAC tries highest-priority first and fails over.
- **Contact rewrite**: in-process mock UAS that responds with `received=`/`rport=` differing from request source; assert next REGISTER carries the rewritten Contact.
- **RFC 4733 DTMF**: extend `audio_roundtrip_integration` with a DTMF event mid-call; assert receiver decodes the digit.
- **Asterisk / FreeSWITCH end-to-end**: a `tests/asterisk_interop/` directory that spins up an Asterisk container in docker-compose and runs the example suite against it. Optional but the only way to catch real-world quirks before users do.

---

## Progress log

| Date | Item | Notes |
|------|------|-------|
| 2026-04-20 | doc | Initial plan written from audit of `sip-transport`, `media-core`, `rtp-core`, `sip-core`, `dialog-core`. |
| 2026-04-20 | B1 sip-core | PAssertedIdentity + PPreferredIdentity typed headers + parser + builder ext landed in `sip-core` (RFC 3325). 18 new unit tests; full sip-core suite still green (1997/1997). dialog-core + session-core wiring tracked as separate follow-up. |
| 2026-04-20 | B1 dialog-core + session-core | Plumbed PAI all the way through. dialog-core gained `make_call_with_extra_headers[_for_session]` + `send_initial_invite_with_extra_headers` (mirrors `send_invite_with_auth`). session-core gained `Config::pai_uri`, `UnifiedCoordinator::make_call_with_pai`, `SessionState.pai_uri`, and `Action::SendINVITE` now routes through `DialogAdapter::send_invite_with_extra_headers` when PAI is set. Inbound PAI surfaces on `IncomingCallInfo.p_asserted_identity` via the existing event_hub headers-map path. Workspace builds clean; sip-core 1997 / dialog-core 174 / session-core 18 lib tests still green. Wire-level multi-binary integration test deferred. |
| 2026-04-20 | A2 ✅ | TCP enable + URI-aware transport selection. `MultiplexedTransport` (`crates/dialog-core/src/transaction/transport/multiplexed.rs`) wraps the per-flavour registry; `TransactionManager::with_transport_manager` installs it as the single `Arc<dyn Transport>`, dispatching by Request-URI scheme + `;transport=` parameter (RFC 3261 §18.1.1, §19.1.5, §26.2). `resolve_uri_to_socketaddr` uses typed `Uri` fields + `sips:`→5061 default + async DNS. 10 multiplexer unit tests (6 selection + 4 dispatch). dialog-core 184/184 + session-core 18/18 + audio_roundtrip still green. |
| 2026-04-21 | A1 ✅ | TLS Steps 1B + 1C — TLS client connector (replaced placeholder at `crates/sip-transport/src/transport/tls/mod.rs` with ~600-LOC working server+client impl using `tokio_rustls::TlsConnector`; `rustls-native-certs` + `webpki-roots` fallback + optional extra CA + dev-only insecure-skip; `bind_with_client_config(addr, cert, key, event_tx, client_cfg)`). `TransportManager` enables TLS via `add_tcp_transport(addr, true)` with `bind_addr.port() + 1` (RFC 3261 5060→5061 convention). session-core `Config::tls_cert_path` / `tls_key_path` / `tls_extra_ca_path` / `tls_insecure_skip_verify`; auto-flips `enable_tls` when cert+key both set. Critical response-routing fix: new `Transport::has_connection_to(addr)` method; `MultiplexedTransport::pick_transport` for Responses probes connection-oriented transports for a live connection before falling back to UDP (RFC 3261 §17.2 / §18.2.2). Tests: `crates/sip-transport/tests/tls_handshake_test.rs` (handshake-then-REGISTER round-trip with `rcgen` self-signed cert + default-validation rejects self-signed) + `crates/session-core/tests/tls_call_integration.rs` (real `sips:` call between two `UnifiedCoordinator`s). |
| 2026-04-21 | B1 wire-level test gap closed | Inbound PAI on `IncomingCallInfo.p_asserted_identity` exercised end-to-end through TLS test path; PAI surfaces correctly on the receiving side. |
| 2026-04-22 | B2 ✅ | RFC 4568 SDES-SRTP — full end-to-end across four phases (2B.1 SDP refactor + `SrtpNegotiator`; 2B.2 wire encryption via `UdpRtpTransport::set_srtp_contexts` + media-core `install_srtp_contexts`; 2B.3 in-process `srtp_call_integration` test; 2B.4 public-API exposure). sip-core CRLF fix + `a=crypto:` parser. session-core media adapter refactored from format-strings to `SdpBuilder` + `SdpSession::from_str` (D11/D12). Two-key asymmetric SDES (D4); RFC 3711 §3.4 silent-drop on auth failure (D7); `Config::offer_srtp` / `srtp_required` / `srtp_offered_suites` exposed publicly. Tests: sip-core 2004 + rtp-core udp 12 + session-core lib 30 + audio_roundtrip + tls_call + srtp_call all green. See `crates/STEP_2B_SRTP_INTEGRATION_PLAN.md`. |
| 2026-04-22 | A3 ✅ | RFC 3581 NAT discovery + RFC 5626 §5 Contact rewrite. `DialogManager::nat_discovered_addr` cache populated by `ResponseHandler::handle_response_message` from typed `Param::Received` + `Param::Rport(Some(_))` on every inbound response (no-op suppression when discovered == local). `UnifiedDialogApi::discovered_public_addr()` exposes the cache. `DialogAdapter::send_register` pre-rewrites Contact host:port via `rewrite_contact_host` helper (preserves scheme + user-part + URI params). 5 NAT-discovery extraction tests + 5 Contact-rewrite tests. dialog-core 189/189 + session-core lib 35/35 + all integration suites green. |
| 2026-04-22 | Sprint 1 ✅ | A1 (TLS) + A2 (TCP+URI selection) + A3 (NAT discovery + Contact rewrite) + B1 (PAI) + B2 (SDES-SRTP) all shipped. session-core is now production-credible against TLS-required cloud carriers (Twilio/Vonage/Bandwidth/Teams Direct Routing), modern Asterisk/FreeSWITCH with `srtp=mandatory`, PBX trunks requiring PAI, and UAs behind NAT registering through carrier SBCs. Outstanding (separate workstreams): A4–A7 (SRV/NAPTR, SIP Outbound, STUN, digest enhancements), B3–B5 (Service-Route, RFC 4733 DTMF, compact headers RX), Tier C (media polish), Tier D (WebRTC), Tier E (advanced/aesthetic). |
| 2026-04-22 | B5 ✅ | Sprint 2 audit found B5 already shipped — `sip-core/src/types/headers/header_name.rs:232-305` `FromStr` maps all RFC 3261 §7.3.3 compact forms (`i/m/e/l/c/f/s/k/t/v`) + later-RFC additions (`o` RFC 6665 Event, `r` RFC 3515 Refer-To, `b` RFC 3892 Referred-By, `x` RFC 4028 Session-Expires) case-insensitively. Fixed one miswiring: `u` was mapped to `Unsupported`; IANA registry + RFC 6665 §7.2 assign `u` to `Allow-Events`. Added `test_compact_header_forms_rfc3261_and_later` covering all compact forms both cases. sip-core 2005/2005 + dialog-core 189/189 green. |
| 2026-04-22 | B3 ✅ core | RFC 3608 Service-Route — full header stack in sip-core (new `ServiceRoute` type at `crates/sip-core/src/types/service_route.rs`, builder ext at `crates/sip-core/src/builder/headers/service_route.rs`, `HeaderName::ServiceRoute` + `TypedHeader::ServiceRoute` wiring including parser dispatch, Display impl, and appendable-header classification in request/response builders) with 13 new unit tests. dialog-core `DialogManager::service_route_by_aor` cache populated by `record_service_route_from_response` from `handle_response_message` (gated on CSeq method = REGISTER + 2xx status + To URI as AoR key); `UnifiedDialogApi::service_route_for_aor` exposes it. 5 extraction unit tests (`extracts_service_route_on_register_200`, `returns_empty_vec_when_register_200_has_no_service_route`, `ignores_non_2xx_register_responses`, `ignores_non_register_responses`, `concatenates_multiple_service_route_headers`). sip-core 2018/2018 + dialog-core 194/194 + session-core 35/35 green. Preload into outbound dialog route_set is a follow-up. |
| 2026-04-22 | A4 ✅ core | RFC 3263 SRV + A/AAAA resolution. `hickory-resolver 0.24` added as workspace dep + dialog-core dep. New `crates/dialog-core/src/dialog/dns_resolver.rs` implementing RFC 3263 §4: (1) IP literal short-circuit, (2) hostname-with-explicit-port skips SRV, (3) hostname-without-port does `_service._proto.host` SRV (`_sip._udp`/`_sip._tcp`/`_sips._tcp` based on URI transport/scheme per RFC 3263 §4.2 + RFC 7118 for ws/wss) with RFC 2782 weighted selection within the lowest-priority group, (4) A/AAAA fallback. New `ResolvedTarget { addr, transport }` surface; back-compat `resolve_uri_to_socketaddr` wrapper preserved so existing A3-era callers (`dialog-core/src/api/unified.rs::send_register`, `transaction/manager/handlers.rs::determine_ack_destination`, `dialog/dialog_impl.rs::remote_target_addr`) compile unchanged. Process-wide `TokioAsyncResolver::tokio_from_system_conf()` cached in a `OnceCell`; logs a one-time warning and degrades to `None` if the system resolver can't be constructed. 15 new unit tests covering SRV service-name derivation across schemes/transports, RFC 2782 weighted selection (zero-weight, weighted, priority gating), scheme-to-default-port, and IP-literal short-circuits. NAPTR deferred — `;transport=` + scheme selection covers the common carrier case, add if a deployment needs NAPTR-based transport discovery. dialog-core 209/209 + session-core lib 35/35 + audio_roundtrip_integration green. |
| 2026-04-23 | A5 Phase 2a ✅ | Outbound-aware REGISTER Contact end-to-end. session-core `Config::sip_outbound_enabled`/`sip_instance`/`outbound_keepalive_interval_secs` added (default off / None / 25 s). `UnifiedCoordinator` boot translates Config → `OutboundContactParams { instance_urn, reg_id: 1 }` when enabled + stable `sip_instance` present (warns and falls back to pre-5626 REGISTER when enabled without instance URN). `DialogAdapter::outbound_contact_params` field threads through to `send_register`. Dialog-core gained `UnifiedDialogApi::send_register_with_outbound_contact` as a sibling of `send_register` that shares the same `send_register_impl` body; new pure `build_outbound_contact(uri, params)` helper produces `<sip:alice@host;ob>;+sip.instance="<urn:uuid:...>";reg-id=N`. 4 new dialog-core unit tests on the Contact shape (including `;ob` inside URI angle brackets, reg-id value propagation, invalid-URI rejection). Legacy `send_register` signature preserved so all existing call sites (1 in session-core, 7 in dialog-core tests) compile unchanged. dialog-core 218/218 + session-core lib 38/38 + audio_roundtrip_integration + register_flow_test all green. A5 Phase 2b (transport CRLFCRLF keep-alive) and Phase 2c (OutboundFlow state machine) remain pending. |
| 2026-04-22 | E4 + E5 + B4 codec + A5 Phase 1 + E1 primitives ✅ | Sprint 2 continuation. **E4** — `Config::outbound_proxy_uri: Option<String>` parsed to `Uri` at `UnifiedCoordinator` construction; `DialogAdapter::outbound_proxy_uri` + pure `prepend_outbound_proxy_route` helper injects `Route: <proxy;lr>` as first header on outgoing INVITEs (3 unit tests); SendINVITE action routes through `send_invite_with_extra_headers` whenever outbound proxy is set even without PAI. **E5** — `format_redirect_or_terminate_reason(response)` in `response_handler.rs` appends alternate Contact URIs to `CallTerminated` reason for 300/301/302/305/380 (5 unit tests). **B4 core** — new `crates/media-core/src/codec/audio/dtmf.rs` with RFC 4733 §2.3 4-byte `TelephoneEvent` encode/decode + `DtmfEvent::from_digit` covering 0-9/`*`/`#`/A-D (case-insensitive); decoder tolerates oversized payloads + ignores reserved R bit; volume saturates at 63 (10 unit tests). **A5 Phase 1 + E1** — new `crates/sip-core/src/types/outbound.rs` with `OutboundContactParams`/`GruuContactParams` + `set/read` helpers enforcing RFC 5626 §4.1 (`+sip.instance="<urn:...>"`) / §4.2 (`reg-id=N`) / §5.4 (`;ob`) and RFC 5627 §5.3 (`pub-gruu`/`temp-gruu`) quoting conventions; idempotent writes (10 unit tests). Tracking doc progress: Sprint 2 row → B3/A4/E4/E5 done, B4 core + A5 Phase 1 + E1 primitives done; B4 wiring + A5 Phase 2+ (OutboundFlow state machine, CRLF keep-alive, REGISTER Contact rewrite) remain. sip-core 2028/2028 + dialog-core 214/214 + media-core 226/226 + session-core lib 38/38 + audio_roundtrip_integration all green. |
| 2026-04-23 | A5 Phase 2b-min ✅ | RFC 5626 §3.5.1 CRLFCRLF keep-alive — stateless-ping shape (Phase 2c adds the flow state machine on top). **Transport trait** (`crates/sip-transport/src/transport/mod.rs`): new `Transport::send_raw(dest, Bytes)` with `NotImplemented` default + two new `TransportEvent` variants (`KeepAlivePongReceived`, `ConnectionClosed { remote_addr, transport_type }`). **TCP** (`crates/sip-transport/src/transport/tcp/connection.rs` + `mod.rs`): new `ReceivedFrame` enum + `receive_frame()`; refactored `TcpConnection` to split the stream into owned read / write halves via `TcpStream::into_split()` (fixes latent mutex contention between reads and writes that would have deadlocked bidirectional flows); leading `\r\n` / `\r\n\r\n` at buffer offset 0 recognised as pong / server-initiated ping and consumed before the SIP parser sees them; server auto-answers pings with CRLF pong; outbound `connect_to` now spawns a reader too (so pongs and responses are surfaced); `ConnectionClosed` emitted before pool eviction; `has_connection_to` implemented (also fixes the latent multiplexer response-routing bug where TCP responses always fell back to UDP); `ConnectionPool::has_connection` helper added. **TLS** (`crates/sip-transport/src/transport/tls/mod.rs`): parallel pong/ping detection in the per-connection reader, auto-pong on ping, `ConnectionClosed` emitted before registry eviction, `send_raw` override via existing per-remote `mpsc::Sender<Bytes>`. **Multiplexer** (`crates/dialog-core/src/transaction/transport/multiplexed.rs`): new `send_raw` override that probes `has_connection_to` across TLS/TCP/WSS/WS (TLS first — matches response-routing order) and dispatches to the first live flow; errors when no connection-oriented transport has the destination. **DialogManager** (`crates/dialog-core/src/manager/core.rs`): new `outbound_ping_tasks: Arc<DashMap<(aor, reg_id, instance), AbortHandle>>` + `outbound_keepalive_interval: RwLock<Option<Duration>>` fields; `set_outbound_keepalive_interval` / `start_outbound_ping` / `stop_outbound_ping` APIs. `start_outbound_ping` spawns a `tokio::time::interval` task that calls `transport.send_raw(dest, b"\r\n\r\n")` and silently terminates on send failure (Phase 2c will upgrade this to a pong-tracking state machine). **REGISTER 2xx hook** (`crates/dialog-core/src/protocol/response_handler.rs`): new `record_outbound_flow_from_response` alongside existing `record_nat_discovery_from_response` / `record_service_route_from_response`; pure `extract_outbound_flow(response)` helper parses echoed Contact for `+sip.instance` / `reg-id` / `;ob`, keys the flow by `(aor, reg_id, instance)`, and pulls the destination from `TransactionManager::transaction_destination(tx_id)` (new public accessor) — gated on REGISTER 2xx with outbound params present, no-op otherwise. **Config threading** (`crates/session-core/src/api/unified.rs`): `Config::outbound_keepalive_interval_secs` (25 s default, added in Phase 2a) now actually threaded — `UnifiedCoordinator` boot calls `dialog_api.dialog_manager().core().set_outbound_keepalive_interval(...)` when outbound is enabled and a stable instance URN is supplied. **Tests**: 4 new TCP connection tests (pong / ping at offset 0, pong-followed-by-message round-trip, send_raw_bytes round-trip); 3 new TCP transport tests (`has_connection_to_reflects_pool`, `send_raw_triggers_server_pong`, `connection_closed_emits_before_pool_eviction`); 3 new multiplexer tests (`send_raw_routes_to_transport_with_live_connection`, `send_raw_errors_when_no_live_connection_exists`, `send_raw_prefers_tls_over_tcp_when_both_live`); 5 new `extract_outbound_flow` unit tests. sip-transport 47/47 + dialog-core 226/226 + session-core lib 38/38 + audio_roundtrip + tls_call + srtp_call + register_flow all green. **Phase 2c** (stateful `OutboundFlow` with pong-timeout detection and failure-driven re-REGISTER) deferred — the stateless ping already delivers the user-visible value (NAT bindings stay alive, SBC idle-timeouts stop killing registrations mid-night). |
| 2026-04-25 | Sprint 2.5 ✅ | Full execution of `MEDIA_PLANE_LAYERING_FOLLOWUPS.md` (Plan B with answered scope-clarifying questions). **P1**: deleted `Action::SendDTMF` / `SendDTMFTone` / `MuteLocalAudio` / `UnmuteLocalAudio` + `EventType::SendDTMF` + their match arms; deleted orphan `event_router.rs` (zero call sites) + its `pub use` export; deleted dead YAML `MuteMicrophone`/`UnmuteMicrophone` transitions; added `strum` dep + `#[derive(VariantNames)]` on `Action`; switched `parse_action_by_name` from silent `Custom` fallthrough to a hard `SessionError` and added `default_yaml_loads_with_no_unknown_actions` + `parse_action_by_name_hard_errors_on_unknown_names` regression tests; mapped previously-fallthrough YAML names (`DestroyMediaBridge`/`UnlinkSessions`/`SendPUBLISH`) to explicit `Action::Custom` arms; added Media-plane side effects + Single SDP-offer entry point sections to `ARCHITECTURE_OVERVIEW.md`. **P6**: new `dev-insecure-tls` Cargo feature in `session-core` + `sip-transport` + `dialog-core` with chained forwarding (`session-core/dev-insecure-tls` → `dialog-core/dev-insecure-tls` → `sip-transport/dev-insecure-tls`); gated `InsecureCertVerifier` + the insecure branch of `build_client_config` behind it (default build doesn't even compile the accept-all type); gated `Config::tls_insecure_skip_verify` field + reader at `api/unified.rs:160` and `:1145`; threaded `false` into `dialog-core/src/transaction/transport/mod.rs:306` under default build; updated `streampeer/tls/{server,client}.rs` with `compile_error!` fallback; `streampeer/tls/run.sh` now passes `--features dev-insecure-tls`; `tls_call_integration` gated with `#[cfg(feature = "dev-insecure-tls")]`. **P5 (reduced scope)**: `state_table::DialogId` preserved unchanged as the `Copy` Uuid newtype with bidirectional `From` impls to `rvoip_dialog_core::DialogId` — full doc-prescribed unification would have broken `Copy`/`Serialize`/dialog-core interop. Only `MediaSessionId` was collapsed: added `Serialize/Deserialize` derives + `new_v4()` to `rvoip_media_core::DialogId`; replaced `pub struct MediaSessionId(pub String)` with `pub type MediaSessionId = rvoip_media_core::DialogId`; fixed ~6 call sites (`MediaSessionId::new()` → `new_v4()`, `MediaSessionId(string)` → `MediaSessionId::new(string)`, `media_id.0` → `media_id.as_str()`); `MediaSessionId::from_dialog` reconstructions in production paths collapsed to identity; new alias-invariant test. **P2**: deleted `MediaAdapter::generate_sdp_offer` (lines 247-302); rewrote `generate_local_sdp` as the sole `SdpBuilder`-driven entry point that always advertises PCMU + PCMA + telephone-event regardless of profile (`RTP/AVP` or `RTP/SAVP`) — fixes a pre-existing bug where the non-SRTP code path's hardcoded `format!` block emitted `m=audio N RTP/AVP 0 8` with no PT 101, silently breaking DTMF negotiation for any plaintext call since Sprint 2; mirrored shape on `negotiate_sdp_as_uas`; new regression test `offer_advertises_telephone_event_on_plaintext`; updated existing `offer_matches_legacy_format_byte_for_byte` / `srtp_offer_uses_savp_profile_and_carries_crypto_lines` golden fixtures to the unified `0 8 101` shape. **P4**: added `dashmap` dep to rtp-core; added `dtmf_seen: Arc<DashMap<(SocketAddr, u32, u32), Instant>>` field to `UdpRtpTransport` + 500 ms TTL + dedup at the PT 101 branch with inline `retain` to bound memory; deleted `dtmf_last_delivered: Arc<RwLock<Option<(u32, u32)>>>` + check + write in `media-core/src/relay/controller/mod.rs::spawn_rtp_event_handler`; new tests `test_pt101_three_end_of_event_retransmits_dedup_to_one_event` + `test_pt101_dedup_scoped_by_peer_addr` (the latter validates the `(peer, ssrc, ts)` keying matters for B2BUA-style multi-stream cases). **P3**: added `RtpScheduler::next_sequence()` + reworked `RtpSession::send_packet_with_pt` to bypass the scheduler's queueing path so the caller's explicit timestamp survives (the scheduler was silently overwriting `packet.header.timestamp` with `self.timestamp`, which broke RFC 4733 §2.1 audio-anchoring); added `RtpSession::current_timestamp()` deriving from wall-clock at `clock_rate` Hz so successive DTMF tones get distinct timestamps even when no audio packets are flowing through the scheduler (the streampeer/dtmf example exercises exactly this case); new `crates/media-core/src/relay/controller/dtmf_transmitter.rs` (~200 LOC) implementing the full RFC 4733 §2.5.1.3 schedule (start E=0 marker=1 → 20 ms continuations with growing `duration` → 3× E=1 retransmits all sharing `start_timestamp`); thinned `MediaSessionController::send_dtmf_packet` to a fire-and-forget `tokio::spawn` of the transmitter; 3 new transmitter unit tests (start packet shape, continuation timestamp+duration invariants, end-of-event retransmits collapse to one). **Cross-phase**: fixed pre-existing inline-`Config { … }` literal initializers in 6 integration tests that had been silently broken since fields were added prior to Sprint 2.5 (now use `Config::local` + override). Aggregate test counts: rtp-core 203/203 + media-core 229/229 (3 new) + session-core lib 41/41 (2 new) + 29 session-core integration test binaries all green; `streampeer/dtmf/run.sh` (5/5), `streampeer/tls/run.sh`, `streampeer/srtp/run.sh`, `streampeer/audio/run.sh` end-to-end green; `examples/run_all.sh` aggregate green. Reduced-scope rationale lives in `MEDIA_PLANE_LAYERING_FOLLOWUPS.md` (success criterion #3 corrected to reflect what shipped). |
| 2026-04-25 | Sprint 3.5 ✅ | **Consume the C2 matcher.** session-core's `negotiate_sdp_as_uas` swapped over to call `rvoip_dialog_core::sdp::match_offer` via a new pure helper `compute_answer_formats(offer, comfort_noise_enabled, strict, offer_srtp, srtp_required) → Result<Vec<String>>`. New `Config::strict_codec_matching` (default `true`) — out of the box the answer is the strict RFC 3264 §6 intersection of offered formats and our supported set, in offerer-preference order; set to `false` for a one-line escape hatch back to the pre-Sprint-3.5 permissive "always answer with full set" shape. No-overlap surfaces as `SDPNegotiationFailed` (state machine → `488 Not Acceptable Here`, matching the existing `srtp_required` rejection path). The hardcoded `0 8 (13) 101` answer block in `negotiate_sdp_as_uas` is gone — rtpmap/fmtp emission is now driven by the negotiated format list, so dropped formats no longer leak rtpmap lines. **Why now (rather than waiting for Sprint 4 D2):** closes C2's actual stated goal, avoids a second invasive pass through the SRTP-aware answer path during Sprint 4, bounded interop risk via the `strict_codec_matching` flag. **Tests**: 4 new helper unit tests (`strict_default_answers_with_intersection_only`, `permissive_mode_answers_with_full_set`, `strict_default_no_overlap_returns_negotiation_failed`, `strict_with_cn_enabled_keeps_cn_in_intersection`) — session-core lib 52/52. Verification: `audio_roundtrip_integration` + `srtp_call_integration` + `tls_call_integration` + streampeer audio/dtmf/srtp examples all green. C1 VAD-driven audio-loop gating remains a deferred follow-up — pull when a deployment asks for the bandwidth savings. |
| 2026-04-25 | Sprint 3 ✅ | **Shipped.** Four-phase execution per `~/.claude/plans/we-just-finished-implemnting-immutable-pizza.md`. **Phase 1 (A7)**: rewrote `auth-core/src/sip_digest.rs` — extended `DigestAlgorithm` with `MD5Sess`/`SHA256Sess`, added `sha2` dep + algorithm-aware hash dispatch, new additive `DigestClient::compute_response_with_state(nc, body)` + `format_authorization_with_state` (legacy entry points kept as thin wrappers); session-core `SessionState::digest_nc: HashMap<(String, String), u32>` keyed by (realm, nonce); both `SendINVITEWithAuth` and `dialog_adapter::send_register` now bump the counter via `entry().and_modify().or_insert(1)` and pass it through; INVITE folds `local_sdp` bytes into HA2 when challenge offers `auth-int`. 13 auth-core lib tests + 2 session-core unit tests on the counter idiom. **Phase 2 (A6)**: hand-rolled RFC 8489 STUN codec (~250 LOC) at new `crates/rtp-core/src/network/stun/{mod,message}.rs`; `StunClient` async surface that shares the bound RTP socket (load-bearing invariant: same socket → same NAT binding); `Config::media_public_addr` (static override) and `Config::stun_server` (probe at coordinator boot, soft-fail with 1500 ms total budget); `MediaAdapter::public_rtp_addr` overrides `c=`/`o=`/`m=audio` when set on both offer and answer paths, with port-zero meaning "advertise IP only, keep local port". 10 STUN codec unit tests + 3 STUN loopback integration tests + 3 media-adapter override unit tests. **Phase 3 (C1)**: PT 13 codec constant added; new `crates/media-core/src/relay/controller/cn_transmitter.rs` mirrors `dtmf_transmitter.rs` shape (one-shot `send(level)` emitting a single PT 13 packet with one-byte payload); `Config::comfort_noise_enabled` threads through to `MediaAdapter::comfort_noise_enabled`; SDP offer + answer advertise `13` between PCMA and telephone-event with `a=rtpmap:13 CN/8000` when enabled. 2 transmitter unit tests + 2 SDP-shape unit tests (advertise / omit). **Scope cut for C1**: VAD-driven gating of the audio transmit loop deferred — the transmitter primitive lands, callers can drive CN from their own VAD, and the audio loop refactor is a follow-up. **Phase 4 (C2)**: generic RFC 3264 §6 matcher at `crates/dialog-core/src/sdp/negotiation.rs` (~340 LOC, was a `// TODO` stub); `match_offer(offer, &AnswerCapabilities) → OfferAnswerMatch` with per-`m=`-line accept/reject, intersection in offerer-preference order (RFC 3264 §6 paragraph 3), `accept_srtp` / `require_srtp` policy gates, `rtpmap`/`fmtp`/`rtcp-fb` filtered to surviving formats, all other attributes (ice-ufrag/ice-pwd/fingerprint/setup/candidate/mid/ssrc/group/msid/extmap/...) carried through verbatim so Sprint 4 D2/D3/D5 consumers extend the matcher rather than splay a third hardcoded SDP shape. 11 integration tests in `crates/session-core/tests/sdp_matcher_integration.rs`. **Scope cut for C2**: session-core's `negotiate_sdp_as_uas` not yet swapped over to consume the matcher — the existing path is SRTP-aware and the swap is a follow-up; the matcher exists, is well-tested, and Sprint 4 D2/D3/D5 can build on it directly. The pre-existing dialog-core `--test`-profile `StableCrateId` collision around `rvoip-infra-common` is unrelated to Sprint 3 scope; the matcher's tests live one crate over to dodge it. **Aggregate test counts**: auth-core lib 13/13 (was 5, +8 new); rtp-core lib 213/213 (10 new STUN codec tests) + 3 STUN loopback integration; media-core lib 231/231 (2 new CN transmitter); session-core lib 48/48 (5 new — 2 digest_nc, 3 public_rtp_addr override) + 11 sdp_matcher_integration; full streampeer audio + DTMF roundtrip green. |
| 2026-04-25 | Sprint 3 kickoff 🚧 | Sprint 3 (media polish) plan committed at `~/.claude/plans/we-just-finished-implemnting-immutable-pizza.md`. Execution order **A7 → A6 → C1 → C2**, four phases with per-phase verification gates mirroring Sprint 2.5. **Phase 1 (A7)** scope: per-(realm, nonce) NC counter via `SessionState::digest_nc`; `auth-int` qop selection when challenge offers it; SHA-256 + SHA-256-sess hash + HA1 paths; additive `compute_response_with_state(nc, body)` overload preserves the legacy `compute_response` signature. **Phase 2 (A6)** scope: hand-rolled RFC 8489 STUN codec in new `crates/rtp-core/src/network/stun/`; `StunClient` shares the bound RTP socket; `Config::media_public_addr` (manual override) and `Config::stun_server` (probe at coordinator boot, soft-fail on timeout). **Phase 3 (C1)** scope: PT 13 codec entry; new `cn_transmitter.rs` mirroring `dtmf_transmitter.rs`; audio loop gates on existing VAD when `Config::comfort_noise_enabled`. **Phase 4 (C2)** scope: generic RFC 3264 §6 `match_offer()` in `crates/dialog-core/src/sdp/matcher.rs`; session-core's `negotiate_sdp_as_uas` swaps over; `Config::strict_codec_matching` default true (RFC-correct), set false to recover today's permissive "always answer with full PCMU+PCMA+101 set" behavior if a deployment breaks. **Deferred to backlog**: SHA-512-256, MD5-sess (rare), STUN long-term credential auth (Sprint 4 D3 ICE prerequisite), persisted nc counter across process restart (sessions are ephemeral). |
| 2026-04-23 | A5 Phase 2c + B4 wiring ✅ | **Sprint 2 closed.** A5 Phase 2c: stateful `OutboundFlow` state machine in new `crates/dialog-core/src/manager/outbound_flow.rs` (~210 LOC) with `FlowState` enum (Idle / AwaitingPong / Failed), idempotent `mark_failed()`, and pong-overdue detection. `DialogManager::outbound_ping_tasks` replaced by `outbound_flows` + `outbound_flow_tasks` + `flow_by_destination` secondary index; `start_outbound_ping` now spawns a `tokio::select!` loop that arms a pong deadline after each CRLFCRLF ping and emits `SessionCoordinationEvent::OutboundFlowFailed { aor, reg_id, instance, reason: FlowFailureReason }` (new variant) exactly once per flow instance. New `on_pong_received` / `on_connection_closed` handlers route transport-side events into the flow state machine via a new `FlowTransportEvent` channel installed by `TransactionManager::set_flow_event_sender` — consumer spawned by `DialogManager::with_global_events`. Cross-crate: `DialogToSessionEvent::OutboundFlowFailed` added to infra-common; session-core's `SessionCrossCrateEventHandler` gained `outbound_flow_last_refresh: DashMap<aor, Instant>` for a 1s debounce, `handle_outbound_flow_failed` finds the matching session by `local_uri == aor` and dispatches `EventType::RefreshRegistration`. `DialogManager::stop()` aborts every outstanding flow task on teardown. B4 wiring: `RtpEvent::DtmfEvent` variant + PT 101 branch in `crates/rtp-core/src/transport/udp.rs` (decoded inline to avoid rtp-core → media-core dependency inversion); `RtpSessionEvent::DtmfReceived` forwarding in `crates/rtp-core/src/session/mod.rs`. media-core: new `DtmfNotification` type + `dtmf_callbacks: DashMap<DialogId, mpsc::Sender<DtmfNotification>>` on `MediaSessionController`; `spawn_rtp_event_handler` dedupes the three RFC 4733 §2.5.1.3 retransmits (track last `(ssrc, duration)` per dialog) and fires the callback once per digit on the first `E=1` frame. session-core media_adapter: `global_coordinator` field + `install_dtmf_callback` per-session bridge publishes `Event::DtmfReceived { call_id, digit }` directly on the API event bus. Outbound: new `RtpSession::send_packet_with_pt` overrides PT; `MediaSessionController::send_dtmf_packet` encodes RFC 4733 `TelephoneEvent` and sends via PT 101; `MediaAdapter::send_dtmf` / `send_dtmf_rfc4733` wire the existing `UnifiedCoordinator::send_dtmf(session_id, digit)` through to real wire transmission (previously a TODO stub). SDP advertisement was already present (`a=rtpmap:101 telephone-event/8000` + `a=fmtp:101 0-15`) at `crates/session-core/src/adapters/media_adapter.rs` offer + answer builders. **Tests**: 7 new outbound_flow state-machine tests + 4 `DialogManager` flow-handler tests (pong lookup, connection-closed event + map cleanup, explicit stop is not a failure event) + 1 rtp-core PT 101 dispatch integration test. sip-core, sip-transport, dialog-core lib 238/238, session-core lib 38/38, rtp-core lib 201/201, media-core lib 225/226 (1 pre-existing flaky benchmark timing test in performance::metrics), + audio_roundtrip_integration all green. |
